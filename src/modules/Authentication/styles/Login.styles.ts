import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  title: {
    fontWeight: '600',
    fontSize: 20,
    lineHeight: 24,
    textAlign: 'center',
    paddingTop: 74,
    paddingBottom: 47,
  },
});

export default styles;
